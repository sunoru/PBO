<UserControl x:Class="LightStudio.PokemonBattle.PBO.Battle.BattleReport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:MinWidth="300">
    <Grid>
        <FlowDocumentScrollViewer Name="reportViewer" IsToolBarVisible="False" BorderThickness="0" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Visible">
            <FlowDocument Name="report" FontSize="15">
                <FlowDocument.Resources>
                    <Style TargetType="Paragraph">
                        <Setter Property="Margin" Value="0"/>
                    </Style>
                </FlowDocument.Resources>
            </FlowDocument>
        </FlowDocumentScrollViewer>
        <Button Content="B" HorizontalAlignment="Right" VerticalAlignment="Center" Click="previousTurn_Click"/>
    </Grid>
</UserControl>
